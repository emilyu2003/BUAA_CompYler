// 测试简单函数
int ga = 10;
int gb = -1024;

int add(int a, int b) {
	int r = a + b;
	return r;
}

int sub(int a, int b) {
	a = a - b;
	return a;
}

int main() {
	a = add(add(10,20), sub(100,10));
	b = sub(add(ga,gc) + add(10,5) / 3, sub(100,add(a,30)));
	printf("5: %d %d\n",a,b);

	return 0;
}